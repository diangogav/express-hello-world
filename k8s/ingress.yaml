apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: express-hello-world
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          service:
            name: express-hello-world
            port:
              number: 80
